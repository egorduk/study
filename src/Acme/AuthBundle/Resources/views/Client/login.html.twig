{% extends "AcmeAuthBundle::layout.html.twig" %}
    {% block content %}
        <div class="container">
            <div class="row">
                <form action="{{ path('client_login') }}" method="post">
                    {{ form_widget(formLogin) }}
                </form>
            </div>
        </div>
    {% endblock %}
